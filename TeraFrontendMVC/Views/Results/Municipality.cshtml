@model IEnumerable<TeraFrontendMVC.Models.MunicipalityViewModel>

@{
    ViewData["Title"] = "Municipality";
}

<h1>Municipalities</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodMun)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodEdo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodMun)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodEdo)
            </td>
        </tr>
}
    </tbody>
</table>
